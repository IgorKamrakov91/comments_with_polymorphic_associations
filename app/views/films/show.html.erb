<h1><%= @film.title %></h1>
<%= image_tag @film.image.url(:medium) %>
<%= simple_format @film.description %>

<%= render partial: 'comments/comment', locals: {commentable: @film} %>
<%= render partial: 'comments/form', locals: {commentable: @film} %>

<%= link_to 'Edit', edit_film_path(@film) %>
<%= link_to 'Back', film_path %>
